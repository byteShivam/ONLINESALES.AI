To run the script:

Save the script to a file, e.g., script.sh.
Open a terminal and navigate to the directory where the script is saved.
Run the command chmod +x script.sh to make the script executable.
Execute the script by running ./script.sh <input> (replace <input> with the desired integer value).